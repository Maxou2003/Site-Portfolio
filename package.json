{
  "name": "portfolio-maxence",
  "version": "1.0.0",
  "description": "Portfolio fullstack",
  "scripts": {
    "setup": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "vite",
    "build": "vite build",
    "backend": "npm run dev -w backend",
    "frontend": "npm run dev -w frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "axios": "^1.6.0",
    "email-validator": "^2.0.4",
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
